# AI Query Agent - Kiến Trúc Hệ Thống v3

> Enterprise-grade Natural Language to SQL Agent inspired by **Uber FINCH**

## Tổng Quan

AI Query Agent là hệ thống chuyển đổi câu hỏi ngôn ngữ tự nhiên thành SQL queries, sử dụng kiến trúc multi-agent, RAG, và enterprise features.

## Kiến Trúc Enterprise (FINCH-Grade)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         USER INTERFACES                                  │
│  ┌─────────┐  ┌─────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │  Web UI │  │  Slack  │  │Google Sheet │  │   API/SDK   │             │
│  └────┬────┘  └────┬────┘  └──────┬──────┘  └──────┬──────┘             │
└───────┼────────────┼──────────────┼────────────────┼────────────────────┘
        └────────────┴──────────────┴────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────────────┐
│                      🧠 AI GATEWAY                                       │
│  • Multi-provider (OpenAI, Gemini, Claude, Ollama)                      │
│  • Response caching • Rate limiting • Fallback • Cost tracking          │
└─────────────────────────────┬───────────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────────────┐
│                     🎯 SUPERVISOR AGENT (LangGraph)                      │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐             │
│  │ Intent Router  │  │ Conv. Memory   │  │ Query Cache    │             │
│  └────────────────┘  └────────────────┘  └────────────────┘             │
└──────┬──────────┬──────────┬──────────┬──────────┬──────────────────────┘
       │          │          │          │          │
┌──────▼───┐ ┌────▼────┐ ┌───▼────┐ ┌───▼────┐ ┌───▼────┐
│  SQL     │ │ Report  │ │Insight │ │  Viz   │ │ Valid  │
│  Writer  │ │ Agent   │ │ Agent  │ │ Agent  │ │ Agent  │
└──────────┘ └─────────┘ └────────┘ └────────┘ └────────┘
       │
┌──────▼──────────────────────────────────────────────────────────────────┐
│                     🔧 SQL AGENT TOOLSET                                 │
│  Column Finder │ Value Finder │ Table Rules │ Execute SQL               │
└─────────────────────────────────────────────────────────────────────────┘
       │
┌──────▼──────────────────────────────────────────────────────────────────┐
│               📚 KNOWLEDGE LAYER                                         │
│  Vector Store │ Schema Manager │ Semantic Layer │ Feedback Store        │
└─────────────────────────────────────────────────────────────────────────┘
       │
┌──────▼──────────────────────────────────────────────────────────────────┐
│               💾 DATABASE LAYER                                          │
│  SQLite │ MySQL │ PostgreSQL │ SQL Server │ Elasticsearch │ OpenSearch  │
└─────────────────────────────────────────────────────────────────────────┘
```

## Enterprise Features

### 1. AI Gateway (`services/ai_gateway.py`)
| Feature | Description |
|---------|-------------|
| Multi-Provider | OpenAI, Gemini, Claude, Ollama |
| Caching | In-memory LRU cache với TTL |
| Rate Limiting | Per-user token bucket |
| Fallback | Auto-switch khi primary fail |
| Cost Tracking | Estimate cost per request |

### 2. Conversation Memory (`services/memory.py`)
- Multi-turn context across messages
- Entity extraction (tables, values, time)
- Session management
- Context window for LLM

### 3. Feedback Learning (`services/feedback.py`)
- Store SQL corrections
- Track query rating
- Export to vector store
- Self-improvement suggestions

### 4. Query Cache (`services/cache.py`)
- Fast repeated queries
- TTL expiration
- LRU eviction

## Agent Types

| Agent | File | Intent | Function |
|-------|------|--------|----------|
| **SQL Writer** | `sql_writer.py` | data_retrieval | Text-to-SQL với toolset |
| **Report** | `report_agent.py` | report_generation | Predefined reports |
| **Insight** | `insight_agent.py` | insight_generation | Variance, trends |
| **Visualization** | `visualization_agent.py` | - | Auto charts |
| **Validation** | `validation_agent.py` | - | SQL safety |

## Flow Xử Lý v3

```
User: "Why did revenue drop last month in Hanoi?"
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│ 1. AI GATEWAY                                            │
│    → Check cache (miss)                                  │
│    → Rate limit OK                                       │
└───────────────────────────────┬─────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────┐
│ 2. SUPERVISOR                                            │
│    → Load conversation memory                            │
│    → Context: user asked about revenue before            │
└───────────────────────────────┬─────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────┐
│ 3. INTENT ANALYSIS                                       │
│    → Intent: insight_generation                          │
│    → Route to: Insight Agent                             │
└───────────────────────────────┬─────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────┐
│ 4. INSIGHT AGENT                                         │
│    → Generate comparison SQLs                            │
│    → Analyze variance                                    │
└───────────────────────────────┬─────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────┐
│ 5. VISUALIZATION AGENT                                   │
│    → Auto-select: Bar chart                              │
│    → Generate Chart.js config                            │
└───────────────────────────────┬─────────────────────────┘
                                ▼
┌─────────────────────────────────────────────────────────┐
│ 6. FEEDBACK & CACHE                                      │
│    → Record query for learning                           │
│    → Cache result                                        │
│    → Update memory                                       │
└─────────────────────────────────────────────────────────┘
```

## Cấu Trúc Source

```
AIQuery/
├── main.py
├── config.py
├── requirements.txt
│
├── docs/
│   ├── ARCHITECTURE.md      # This file
│   ├── API.md
│   └── DEPLOYMENT.md
│
├── services/                 # Enterprise services
│   ├── ai_gateway.py        # LLM gateway
│   ├── memory.py            # Conversation memory
│   ├── feedback.py          # Learning loop
│   └── cache.py             # Query cache
│
├── agents/
│   ├── supervisor.py        # v3 orchestrator
│   ├── intent_agent.py      # 6 intent types
│   ├── sql_writer.py        # Text-to-SQL
│   ├── report_agent.py      # Reports
│   ├── insight_agent.py     # Analytics
│   ├── visualization_agent.py
│   ├── validation_agent.py
│   └── tools/
│       ├── column_finder.py
│       ├── value_finder.py
│       ├── table_rules.py
│       └── execute_sql.py
│
├── rag/
│   ├── vector_store.py
│   ├── schema_manager.py
│   └── semantic_layer.py
│
├── database/
│   ├── connector.py
│   └── sources/
│       ├── mysql.py
│       ├── postgresql.py
│       ├── sqlserver.py
│       └── elasticsearch.py
│
├── api/routes/
│   ├── query.py
│   ├── schema.py
│   └── history.py
│
└── frontend/
    ├── index.html
    ├── styles.css
    └── app.js
```

## So Sánh với Uber FINCH

| Feature | FINCH | Chúng Ta | Status |
|---------|-------|----------|--------|
| Supervisor Agent | ✅ | ✅ | ✅ |
| 6 Intent Types | ✅ | ✅ | ✅ |
| SQL Agent Toolset | ✅ | ✅ | ✅ |
| Multi-Database | ✅ | ✅ | ✅ |
| AI Gateway | ✅ | ✅ | ✅ |
| Specialized Agents | ✅ | ✅ | ✅ |
| Conversation Memory | ✅ | ✅ | ✅ |
| Feedback Loop | ✅ | ✅ | ✅ |
| Caching | ✅ | ✅ | ✅ |
| Visualization | ✅ | ✅ | ✅ |
